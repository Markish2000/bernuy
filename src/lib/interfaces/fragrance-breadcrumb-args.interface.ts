export interface FragranceBreadcrumbArgs {
  readonly character: string; // display final del breadcrumb (ya traducido)
  readonly fragrancesLabel: string; // label de la colección (ya traducido)
  readonly slug: string;
}
